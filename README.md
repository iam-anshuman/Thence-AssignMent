# Thence Internship Assignment

- The Project is made using `React + Tailwind`
-  To Run The Project clone the repo and run command in terminal `cd Thence-AssignMent` and `npm init` and `npm run dev`
